interface:
  display_name: "CC Team — Claude Code + Codex"
  short_description: "Boot a Claude Code + Codex agent team via TeamCreate"
  default_prompt: |
    Use $cc-team-mini to boot a Claude Code + Codex agent team.
    Read `references/leader.md` first — it contains the full workflow.
    Boot only: use TeamCreate to create the Leader agent, report team ready, then wait for the user to provide a task. Do not start any task automatically.
policy:
  allow_implicit_invocation: false
